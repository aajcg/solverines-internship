import React from 'react';
import '../styles/App.css';

const Quote = () => {
    return (
        <section className="quote">
            <p className="quote-text">Harness rapid application development to transcend inertia and move at the speed of thought</p>
        </section>
    );
};

export default Quote;